Start-Process pwsh -Verb RunAs          # PowerShell Core
Start-Process powershell -Verb RunAs    # Windows PowerShell

Start-Process pwsh -Verb RunAs -ArgumentList '-Command', 'your-command-here'

([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole(
    [Security.Principal.WindowsBuiltInRole]::Administrator
)


Start-Process printmanagement.msc -Verb RunAs

(whoami /groups) -match 'S-1-5-32-544'  # S-1-5-32-544 is the Administrators group SID

([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole(
    [Security.Principal.WindowsBuiltInRole]::Administrator
)
